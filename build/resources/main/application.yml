spring:
  application:
    name: rapidstay-api
  profiles:
    active: local

# ======================================================
# 🏠 Local (개발 PC, 외부 SSL rediss)
# ======================================================
---
spring:
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dpg-d44bbu3ipnbc73d2n6q0-a.oregon-postgres.render.com:5432/rapidstay_db
    username: redshine75
    password: EKj5HOzXrKDY91Y4a5F4rQzDyyL5QWpR

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      url: rediss://red-d40ub0k9c44c73cdgu60:eCCiLBqjN0WLXOQrEzDZahZ3dhTm1PkY@oregon-keyvalue.render.com:6379
      ssl:
        enabled: true
      timeout: 60s
      connect-timeout: 10s

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.rapidstay: DEBUG

web:
  resources:
    add-mappings: false


# ======================================================
# 🚧 Dev (Render 내부 개발 서버 — 내부망 우선)
# ======================================================
---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dpg-d44bbu3ipnbc73d2n6q0-a.oregon-postgres.render.com:5432/rapidstay_db
    username: redshine75
    password: EKj5HOzXrKDY91Y4a5F4rQzDyyL5QWpR

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      # ✅ 내부 연결 (Private Networking)
      url: redis://red-d40ub0k9c44c73cdgu60:eCCiLBqjN0WLXOQrEzDZahZ3dhTm1PkY@red-d40ub0k9c44c73cdgu60.internal:6379
      ssl:
        enabled: false
      timeout: 30s
      connect-timeout: 10s

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.rapidstay: DEBUG


# ======================================================
# 🚀 Prod (Render 운영 서버 — 내부망 연결)
# ======================================================
---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dpg-d44bbu3ipnbc73d2n6q0-a.oregon-postgres.render.com:5432/rapidstay_db
    username: redshine75
    password: EKj5HOzXrKDY91Y4a5F4rQzDyyL5QWpR

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      # ✅ 내부 연결 (운영용)
      url: redis://red-d40ub0k9c44c73cdgu60:eCCiLBqjN0WLXOQrEzDZahZ3dhTm1PkY@red-d40ub0k9c44c73cdgu60.internal:6379
      ssl:
        enabled: false
      timeout: 30s
      connect-timeout: 10s

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.rapidstay: INFO


# ======================================================
# 🩺 Health Check (Render)
# ======================================================
---
management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never
    info:
      enabled: true
  health:
    redis:
      enabled: true
