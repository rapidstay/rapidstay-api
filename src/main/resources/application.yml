spring:
  application:
    name: rapidstay-api
  profiles:
    active: local

# ======================================================
# 🏠 Local
# ======================================================
---
spring:
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dpg-d44bbu3ipnbc73d2n6q0-a.oregon-postgres.render.com:5432/rapidstay_db
    username: redshine75
    password: EKj5HOzXrKDY91Y4a5F4rQzDyyL5QWpR

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 60000

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.rapidstay: DEBUG

web:
  resources:
    add-mappings: false


# ======================================================
# 🚧 Dev (Render / 개발서버)
# ======================================================
---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dpg-d44bbu3ipnbc73d2n6q0-a.oregon-postgres.render.com:5432/rapidstay_db
    username: redshine75
    password: EKj5HOzXrKDY91Y4a5F4rQzDyyL5QWpR

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      url: redis-cli --tls -u redis://default:AYFNAAIncDJmNWU2NDE2OTJlZWY0NzAwYWU5OTE0NzZmODc2MDNmNnAyMzMxMDE@premium-mongrel-33101.upstash.io:6379
      ssl:
        enabled: false
      timeout: 10000
      connect-timeout: 5000

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.rapidstay: DEBUG


# ======================================================
# 🚀 Prod (운영 / Upstash Redis)
# ======================================================
---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dpg-d44bbu3ipnbc73d2n6q0-a.oregon-postgres.render.com:5432/rapidstay_db
    username: redshine75
    password: EKj5HOzXrKDY91Y4a5F4rQzDyyL5QWpR

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      url: redis-cli --tls -u redis://default:AYFNAAIncDJmNWU2NDE2OTJlZWY0NzAwYWU5OTE0NzZmODc2MDNmNnAyMzMxMDE@premium-mongrel-33101.upstash.io:6379
      ssl:
        enabled: false
      timeout: 10000
      connect-timeout: 5000

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.rapidstay: INFO


# ======================================================
# 🩺 Health Check (Render)
# ======================================================
---
management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never
    info:
      enabled: true
  health:
    redis:
      enabled: false
